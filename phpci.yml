setup:
  env:
    TEST_DB_DB: "testdb"
    TEST_DB_HOST: "127.0.0.1"
    TEST_DB_USER: "testuser"
    TEST_DB_PWD: "password"

  composer:
    action: "install"

test:
  php_unit:
    config:
      - "phpunit.xml"
    directory:
          - "app/tests/"
  php_loc:
    directory: "app"
