setup:
  env:
    TEST_DB_DB: "sss"
    TEST_DB_HOST: "127.0.0.1"
    TEST_DB_USER: "sss"
    TEST_DB_PWD: "sss"

  composer:
    action: "install"

test:
  php_unit:
    config:
      - "phpunit.xml"
    directory:
          - "app/tests/"
